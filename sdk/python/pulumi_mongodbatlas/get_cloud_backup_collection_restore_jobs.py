# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetCloudBackupCollectionRestoreJobsResult',
    'AwaitableGetCloudBackupCollectionRestoreJobsResult',
    'get_cloud_backup_collection_restore_jobs',
    'get_cloud_backup_collection_restore_jobs_output',
]

@pulumi.output_type
class GetCloudBackupCollectionRestoreJobsResult:
    """
    A collection of values returned by getCloudBackupCollectionRestoreJobs.
    """
    def __init__(__self__, cluster_name=None, project_id=None, results=None):
        if cluster_name and not isinstance(cluster_name, str):
            raise TypeError("Expected argument 'cluster_name' to be a str")
        pulumi.set(__self__, "cluster_name", cluster_name)
        if project_id and not isinstance(project_id, str):
            raise TypeError("Expected argument 'project_id' to be a str")
        pulumi.set(__self__, "project_id", project_id)
        if results and not isinstance(results, list):
            raise TypeError("Expected argument 'results' to be a list")
        pulumi.set(__self__, "results", results)

    @_builtins.property
    @pulumi.getter(name="clusterName")
    def cluster_name(self) -> _builtins.str:
        """
        Human-readable label that identifies the cluster with the collection restore jobs you want to return.
        """
        return pulumi.get(self, "cluster_name")

    @_builtins.property
    @pulumi.getter(name="projectId")
    def project_id(self) -> _builtins.str:
        """
        Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
        """
        return pulumi.get(self, "project_id")

    @_builtins.property
    @pulumi.getter
    def results(self) -> Sequence['outputs.GetCloudBackupCollectionRestoreJobsResultResult']:
        """
        List of returned documents that MongoDB Cloud provides when completing this request.
        """
        return pulumi.get(self, "results")


class AwaitableGetCloudBackupCollectionRestoreJobsResult(GetCloudBackupCollectionRestoreJobsResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetCloudBackupCollectionRestoreJobsResult(
            cluster_name=self.cluster_name,
            project_id=self.project_id,
            results=self.results)


def get_cloud_backup_collection_restore_jobs(cluster_name: Optional[_builtins.str] = None,
                                             project_id: Optional[_builtins.str] = None,
                                             opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetCloudBackupCollectionRestoreJobsResult:
    """
    `get_cloud_backup_collection_restore_jobs` describes all collection restore jobs for a cluster.

    To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.

    ## Example Usage

    The following example lists collection restore jobs on a cluster, then reads one job by `job_id`. When `job_id` is omitted, the example defaults to the last job from the plural data source.

    ```python
    import pulumi
    import pulumi_mongodbatlas as mongodbatlas
    import pulumi_std as std

    # Read back collection restore jobs on a cluster without creating a new job.
    this = mongodbatlas.get_cloud_backup_collection_restore_jobs(project_id=project_id,
        cluster_name=cluster_name)
    jobs = std.coalesce(input=[
        this.results,
        [],
    ])["result"]
    job_id = std.coalesce(input=[
        job_id_config,
        jobs[len(jobs) - 1]["jobId"] if len(jobs) > 0 else None,
    ])["result"]
    pulumi.export("restoreJobs", [{
        "jobId": j["jobId"],
        "state": j["state"],
        "createdAt": j["createdAt"],
    } for j in jobs])
    pulumi.export("jobId", job_id)
    this_get_cloud_backup_collection_restore_job = mongodbatlas.get_cloud_backup_collection_restore_job(project_id=project_id,
        cluster_name=cluster_name,
        job_id=job_id)
    pulumi.export("jobState", this_get_cloud_backup_collection_restore_job.state)
    ```

    ### Further Examples
    - Restore job inspection
    - Snapshot restore
    - Point-in-time restore
    - Snapshot discovery


    :param _builtins.str cluster_name: Human-readable label that identifies the cluster with the collection restore jobs you want to return.
    :param _builtins.str project_id: Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
    """
    __args__ = dict()
    __args__['clusterName'] = cluster_name
    __args__['projectId'] = project_id
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('mongodbatlas:index/getCloudBackupCollectionRestoreJobs:getCloudBackupCollectionRestoreJobs', __args__, opts=opts, typ=GetCloudBackupCollectionRestoreJobsResult).value

    return AwaitableGetCloudBackupCollectionRestoreJobsResult(
        cluster_name=pulumi.get(__ret__, 'cluster_name'),
        project_id=pulumi.get(__ret__, 'project_id'),
        results=pulumi.get(__ret__, 'results'))
def get_cloud_backup_collection_restore_jobs_output(cluster_name: pulumi.Input[Optional[_builtins.str]] = None,
                                                    project_id: pulumi.Input[Optional[_builtins.str]] = None,
                                                    opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetCloudBackupCollectionRestoreJobsResult]:
    """
    `get_cloud_backup_collection_restore_jobs` describes all collection restore jobs for a cluster.

    To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.

    ## Example Usage

    The following example lists collection restore jobs on a cluster, then reads one job by `job_id`. When `job_id` is omitted, the example defaults to the last job from the plural data source.

    ```python
    import pulumi
    import pulumi_mongodbatlas as mongodbatlas
    import pulumi_std as std

    # Read back collection restore jobs on a cluster without creating a new job.
    this = mongodbatlas.get_cloud_backup_collection_restore_jobs(project_id=project_id,
        cluster_name=cluster_name)
    jobs = std.coalesce(input=[
        this.results,
        [],
    ])["result"]
    job_id = std.coalesce(input=[
        job_id_config,
        jobs[len(jobs) - 1]["jobId"] if len(jobs) > 0 else None,
    ])["result"]
    pulumi.export("restoreJobs", [{
        "jobId": j["jobId"],
        "state": j["state"],
        "createdAt": j["createdAt"],
    } for j in jobs])
    pulumi.export("jobId", job_id)
    this_get_cloud_backup_collection_restore_job = mongodbatlas.get_cloud_backup_collection_restore_job(project_id=project_id,
        cluster_name=cluster_name,
        job_id=job_id)
    pulumi.export("jobState", this_get_cloud_backup_collection_restore_job.state)
    ```

    ### Further Examples
    - Restore job inspection
    - Snapshot restore
    - Point-in-time restore
    - Snapshot discovery


    :param _builtins.str cluster_name: Human-readable label that identifies the cluster with the collection restore jobs you want to return.
    :param _builtins.str project_id: Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
    """
    __args__ = dict()
    __args__['clusterName'] = cluster_name
    __args__['projectId'] = project_id
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('mongodbatlas:index/getCloudBackupCollectionRestoreJobs:getCloudBackupCollectionRestoreJobs', __args__, opts=opts, typ=GetCloudBackupCollectionRestoreJobsResult)
    return __ret__.apply(lambda __response__: GetCloudBackupCollectionRestoreJobsResult(
        cluster_name=pulumi.get(__response__, 'cluster_name'),
        project_id=pulumi.get(__response__, 'project_id'),
        results=pulumi.get(__response__, 'results')))
