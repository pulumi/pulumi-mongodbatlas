// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * `mongodbatlas.ApiKeyProjectAssignment` describes an API Key Project Assignment.
 *
 * ## Example Usage
 *
 * ### S
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 *
 * const _this = new mongodbatlas.ApiKey("this", {
 *     orgId: orgId,
 *     description: "Test API Key",
 *     roleNames: ["ORG_READ_ONLY"],
 * });
 * const firstProject = new mongodbatlas.Project("first_project", {
 *     name: "First Project",
 *     orgId: orgId,
 * });
 * const secondProject = new mongodbatlas.Project("second_project", {
 *     name: "Second Project",
 *     orgId: orgId,
 * });
 * const firstAssignmentApiKeyProjectAssignment = new mongodbatlas.ApiKeyProjectAssignment("first_assignment", {
 *     projectId: firstProject.id,
 *     apiKeyId: _this.apiKeyId,
 *     roles: ["GROUP_OWNER"],
 * });
 * const secondAssignment = new mongodbatlas.ApiKeyProjectAssignment("second_assignment", {
 *     projectId: secondProject.id,
 *     apiKeyId: _this.apiKeyId,
 *     roles: ["GROUP_OWNER"],
 * });
 * // Add IP Access List Entry to Programmatic API Key 
 * const thisAccessListApiKey = new mongodbatlas.AccessListApiKey("this", {
 *     orgId: orgId,
 *     cidrBlock: "0.0.0.0/1",
 *     apiKeyId: _this.apiKeyId,
 * });
 * // Data source to read a single API key project assignment
 * const firstAssignment = mongodbatlas.getApiKeyProjectAssignmentOutput({
 *     projectId: firstAssignmentApiKeyProjectAssignment.projectId,
 *     apiKeyId: firstAssignmentApiKeyProjectAssignment.apiKeyId,
 * });
 * // Data source to read all API key project assignments for a project
 * const allAssignments = mongodbatlas.getApiKeyProjectAssignmentsOutput({
 *     projectId: firstProject.id,
 * });
 * export const firstAssignmentProjectId = firstAssignment.apply(firstAssignment => firstAssignment.projectId);
 * export const allAssignmentsProjectIds = allAssignments.apply(allAssignments => .map(assignment => (assignment.projectId)));
 * ```
 */
export function getApiKeyProjectAssignment(args: GetApiKeyProjectAssignmentArgs, opts?: pulumi.InvokeOptions): Promise<GetApiKeyProjectAssignmentResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("mongodbatlas:index/getApiKeyProjectAssignment:getApiKeyProjectAssignment", {
        "apiKeyId": args.apiKeyId,
        "projectId": args.projectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getApiKeyProjectAssignment.
 */
export interface GetApiKeyProjectAssignmentArgs {
    /**
     * Unique 24-hexadecimal digit string that identifies this organization API key that you want to assign to one project.
     */
    apiKeyId: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project. Use the /groups endpoint to retrieve all projects to which the authenticated user has access.
     */
    projectId: string;
}

/**
 * A collection of values returned by getApiKeyProjectAssignment.
 */
export interface GetApiKeyProjectAssignmentResult {
    /**
     * Unique 24-hexadecimal digit string that identifies this organization API key that you want to assign to one project.
     */
    readonly apiKeyId: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project. Use the /groups endpoint to retrieve all projects to which the authenticated user has access.
     */
    readonly projectId: string;
    readonly roles: string[];
}
/**
 * `mongodbatlas.ApiKeyProjectAssignment` describes an API Key Project Assignment.
 *
 * ## Example Usage
 *
 * ### S
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 *
 * const _this = new mongodbatlas.ApiKey("this", {
 *     orgId: orgId,
 *     description: "Test API Key",
 *     roleNames: ["ORG_READ_ONLY"],
 * });
 * const firstProject = new mongodbatlas.Project("first_project", {
 *     name: "First Project",
 *     orgId: orgId,
 * });
 * const secondProject = new mongodbatlas.Project("second_project", {
 *     name: "Second Project",
 *     orgId: orgId,
 * });
 * const firstAssignmentApiKeyProjectAssignment = new mongodbatlas.ApiKeyProjectAssignment("first_assignment", {
 *     projectId: firstProject.id,
 *     apiKeyId: _this.apiKeyId,
 *     roles: ["GROUP_OWNER"],
 * });
 * const secondAssignment = new mongodbatlas.ApiKeyProjectAssignment("second_assignment", {
 *     projectId: secondProject.id,
 *     apiKeyId: _this.apiKeyId,
 *     roles: ["GROUP_OWNER"],
 * });
 * // Add IP Access List Entry to Programmatic API Key 
 * const thisAccessListApiKey = new mongodbatlas.AccessListApiKey("this", {
 *     orgId: orgId,
 *     cidrBlock: "0.0.0.0/1",
 *     apiKeyId: _this.apiKeyId,
 * });
 * // Data source to read a single API key project assignment
 * const firstAssignment = mongodbatlas.getApiKeyProjectAssignmentOutput({
 *     projectId: firstAssignmentApiKeyProjectAssignment.projectId,
 *     apiKeyId: firstAssignmentApiKeyProjectAssignment.apiKeyId,
 * });
 * // Data source to read all API key project assignments for a project
 * const allAssignments = mongodbatlas.getApiKeyProjectAssignmentsOutput({
 *     projectId: firstProject.id,
 * });
 * export const firstAssignmentProjectId = firstAssignment.apply(firstAssignment => firstAssignment.projectId);
 * export const allAssignmentsProjectIds = allAssignments.apply(allAssignments => .map(assignment => (assignment.projectId)));
 * ```
 */
export function getApiKeyProjectAssignmentOutput(args: GetApiKeyProjectAssignmentOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetApiKeyProjectAssignmentResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("mongodbatlas:index/getApiKeyProjectAssignment:getApiKeyProjectAssignment", {
        "apiKeyId": args.apiKeyId,
        "projectId": args.projectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getApiKeyProjectAssignment.
 */
export interface GetApiKeyProjectAssignmentOutputArgs {
    /**
     * Unique 24-hexadecimal digit string that identifies this organization API key that you want to assign to one project.
     */
    apiKeyId: pulumi.Input<string>;
    /**
     * Unique 24-hexadecimal digit string that identifies your project. Use the /groups endpoint to retrieve all projects to which the authenticated user has access.
     */
    projectId: pulumi.Input<string>;
}
