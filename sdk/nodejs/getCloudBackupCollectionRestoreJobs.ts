// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * `mongodbatlas.getCloudBackupCollectionRestoreJobs` describes all collection restore jobs for a cluster.
 *
 * To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.
 *
 * ## Example Usage
 *
 * The following example lists collection restore jobs on a cluster, then reads one job by `jobId`. When `jobId` is omitted, the example defaults to the last job from the plural data source.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 * import * as std from "@pulumi/std";
 *
 * export = async () => {
 *     // Read back collection restore jobs on a cluster without creating a new job.
 *     const _this = await mongodbatlas.getCloudBackupCollectionRestoreJobs({
 *         projectId: projectId,
 *         clusterName: clusterName,
 *     });
 *     const jobs = std.coalesce({
 *         input: [
 *             _this.results,
 *             [],
 *         ],
 *     }).result;
 *     const jobId = std.coalesce({
 *         input: [
 *             jobIdConfig,
 *             jobs.length > 0 ? jobs[jobs.length - 1].jobId : null,
 *         ],
 *     }).result;
 *     const thisGetCloudBackupCollectionRestoreJob = await mongodbatlas.getCloudBackupCollectionRestoreJob({
 *         projectId: projectId,
 *         clusterName: clusterName,
 *         jobId: jobId,
 *     });
 *     return {
 *         restoreJobs: .map(j => ({
 *             jobId: j.jobId,
 *             state: j.state,
 *             createdAt: j.createdAt,
 *         })),
 *         jobId: jobId,
 *         jobState: thisGetCloudBackupCollectionRestoreJob.state,
 *     };
 * }
 * ```
 *
 * ### Further Examples
 * - Restore job inspection
 * - Snapshot restore
 * - Point-in-time restore
 * - Snapshot discovery
 */
export function getCloudBackupCollectionRestoreJobs(args: GetCloudBackupCollectionRestoreJobsArgs, opts?: pulumi.InvokeOptions): Promise<GetCloudBackupCollectionRestoreJobsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("mongodbatlas:index/getCloudBackupCollectionRestoreJobs:getCloudBackupCollectionRestoreJobs", {
        "clusterName": args.clusterName,
        "projectId": args.projectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getCloudBackupCollectionRestoreJobs.
 */
export interface GetCloudBackupCollectionRestoreJobsArgs {
    /**
     * Human-readable label that identifies the cluster with the collection restore jobs you want to return.
     */
    clusterName: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    projectId: string;
}

/**
 * A collection of values returned by getCloudBackupCollectionRestoreJobs.
 */
export interface GetCloudBackupCollectionRestoreJobsResult {
    /**
     * Human-readable label that identifies the cluster with the collection restore jobs you want to return.
     */
    readonly clusterName: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    readonly projectId: string;
    /**
     * List of returned documents that MongoDB Cloud provides when completing this request.
     */
    readonly results: outputs.GetCloudBackupCollectionRestoreJobsResult[];
}
/**
 * `mongodbatlas.getCloudBackupCollectionRestoreJobs` describes all collection restore jobs for a cluster.
 *
 * To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.
 *
 * ## Example Usage
 *
 * The following example lists collection restore jobs on a cluster, then reads one job by `jobId`. When `jobId` is omitted, the example defaults to the last job from the plural data source.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 * import * as std from "@pulumi/std";
 *
 * export = async () => {
 *     // Read back collection restore jobs on a cluster without creating a new job.
 *     const _this = await mongodbatlas.getCloudBackupCollectionRestoreJobs({
 *         projectId: projectId,
 *         clusterName: clusterName,
 *     });
 *     const jobs = std.coalesce({
 *         input: [
 *             _this.results,
 *             [],
 *         ],
 *     }).result;
 *     const jobId = std.coalesce({
 *         input: [
 *             jobIdConfig,
 *             jobs.length > 0 ? jobs[jobs.length - 1].jobId : null,
 *         ],
 *     }).result;
 *     const thisGetCloudBackupCollectionRestoreJob = await mongodbatlas.getCloudBackupCollectionRestoreJob({
 *         projectId: projectId,
 *         clusterName: clusterName,
 *         jobId: jobId,
 *     });
 *     return {
 *         restoreJobs: .map(j => ({
 *             jobId: j.jobId,
 *             state: j.state,
 *             createdAt: j.createdAt,
 *         })),
 *         jobId: jobId,
 *         jobState: thisGetCloudBackupCollectionRestoreJob.state,
 *     };
 * }
 * ```
 *
 * ### Further Examples
 * - Restore job inspection
 * - Snapshot restore
 * - Point-in-time restore
 * - Snapshot discovery
 */
export function getCloudBackupCollectionRestoreJobsOutput(args: GetCloudBackupCollectionRestoreJobsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetCloudBackupCollectionRestoreJobsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("mongodbatlas:index/getCloudBackupCollectionRestoreJobs:getCloudBackupCollectionRestoreJobs", {
        "clusterName": args.clusterName,
        "projectId": args.projectId,
    }, opts);
}

/**
 * A collection of arguments for invoking getCloudBackupCollectionRestoreJobs.
 */
export interface GetCloudBackupCollectionRestoreJobsOutputArgs {
    /**
     * Human-readable label that identifies the cluster with the collection restore jobs you want to return.
     */
    clusterName: pulumi.Input<string>;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    projectId: pulumi.Input<string>;
}
