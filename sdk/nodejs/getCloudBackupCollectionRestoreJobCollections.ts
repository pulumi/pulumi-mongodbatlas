// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * `mongodbatlas.getCloudBackupCollectionRestoreJobCollections` describes the restore state of each collection in a collection restore job.
 *
 * To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.
 *
 * > **Note:** Results are empty while the job is `INITIALIZING`. Collection states exist after the job enters `IN_PROGRESS`.
 *
 * ## Example Usage
 *
 * The following example lists per-collection restore state for a job, then reads one collection by `sourceNamespace`. When `sourceNamespace` is omitted, the example defaults to the last namespace from the plural data source.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 * import * as std from "@pulumi/std";
 *
 * // Per-collection read-back for the resolved collection restore job.
 * const _this = mongodbatlas.getCloudBackupCollectionRestoreJobCollections({
 *     projectId: projectId,
 *     clusterName: clusterName,
 *     jobId: jobId,
 * });
 * const collections = std.coalesce({
 *     input: [
 *         _this.then(_this => _this.results),
 *         [],
 *     ],
 * }).result;
 * const sourceNamespace = std.coalesce({
 *     input: [
 *         sourceNamespaceConfig,
 *         collections.length > 0 ? collections[collections.length - 1].sourceNamespace : null,
 *     ],
 * }).result;
 * export const collectionStates = .map(c => ({
 *     sourceNamespace: c.sourceNamespace,
 *     targetNamespace: c.targetNamespace,
 *     effectiveTargetNamespace: c.effectiveTargetNamespace,
 *     state: c.state,
 *     indexStatus: c.indexStatus,
 *     documents: `${c.restoredDocuments} / ${c.totalDocuments}`,
 * }));
 * const thisGetCloudBackupCollectionRestoreJobCollection = mongodbatlas.getCloudBackupCollectionRestoreJobCollection({
 *     projectId: projectId,
 *     clusterName: clusterName,
 *     jobId: jobId,
 *     sourceNamespace: sourceNamespace,
 * });
 * export const collectionState = thisGetCloudBackupCollectionRestoreJobCollection.then(thisGetCloudBackupCollectionRestoreJobCollection => thisGetCloudBackupCollectionRestoreJobCollection.state);
 * ```
 *
 * ### Further Examples
 * - Restore job inspection
 * - Snapshot restore
 * - Point-in-time restore
 * - Snapshot discovery
 */
export function getCloudBackupCollectionRestoreJobCollections(args: GetCloudBackupCollectionRestoreJobCollectionsArgs, opts?: pulumi.InvokeOptions): Promise<GetCloudBackupCollectionRestoreJobCollectionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("mongodbatlas:index/getCloudBackupCollectionRestoreJobCollections:getCloudBackupCollectionRestoreJobCollections", {
        "clusterName": args.clusterName,
        "jobId": args.jobId,
        "projectId": args.projectId,
        "sourceNamespace": args.sourceNamespace,
        "state": args.state,
        "targetNamespace": args.targetNamespace,
    }, opts);
}

/**
 * A collection of arguments for invoking getCloudBackupCollectionRestoreJobCollections.
 */
export interface GetCloudBackupCollectionRestoreJobCollectionsArgs {
    /**
     * Human-readable label that identifies the cluster with the collection restore job you want to return.
     */
    clusterName: string;
    /**
     * Unique 24-hexadecimal digit string that identifies the collection restore job.
     */
    jobId: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    projectId: string;
    /**
     * Source namespace to filter by (e.g. `db.collection`).
     */
    sourceNamespace?: string;
    /**
     * Collection-level state to filter by.
     */
    state?: string;
    /**
     * Target namespace to filter by (e.g. `db.collection`).
     */
    targetNamespace?: string;
}

/**
 * A collection of values returned by getCloudBackupCollectionRestoreJobCollections.
 */
export interface GetCloudBackupCollectionRestoreJobCollectionsResult {
    /**
     * Human-readable label that identifies the cluster with the collection restore job you want to return.
     */
    readonly clusterName: string;
    /**
     * Unique 24-hexadecimal digit string that identifies the collection restore job.
     */
    readonly jobId: string;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    readonly projectId: string;
    /**
     * List of returned documents that MongoDB Cloud provides when completing this request.
     */
    readonly results: outputs.GetCloudBackupCollectionRestoreJobCollectionsResult[];
    /**
     * Source namespace to filter by (e.g. `db.collection`).
     */
    readonly sourceNamespace?: string;
    /**
     * Collection-level state to filter by.
     */
    readonly state?: string;
    /**
     * Target namespace to filter by (e.g. `db.collection`).
     */
    readonly targetNamespace?: string;
}
/**
 * `mongodbatlas.getCloudBackupCollectionRestoreJobCollections` describes the restore state of each collection in a collection restore job.
 *
 * To use this data source, the requesting Service Account or API Key must have the Backup Manager or Project Owner role.
 *
 * > **Note:** Results are empty while the job is `INITIALIZING`. Collection states exist after the job enters `IN_PROGRESS`.
 *
 * ## Example Usage
 *
 * The following example lists per-collection restore state for a job, then reads one collection by `sourceNamespace`. When `sourceNamespace` is omitted, the example defaults to the last namespace from the plural data source.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as mongodbatlas from "@pulumi/mongodbatlas";
 * import * as std from "@pulumi/std";
 *
 * // Per-collection read-back for the resolved collection restore job.
 * const _this = mongodbatlas.getCloudBackupCollectionRestoreJobCollections({
 *     projectId: projectId,
 *     clusterName: clusterName,
 *     jobId: jobId,
 * });
 * const collections = std.coalesce({
 *     input: [
 *         _this.then(_this => _this.results),
 *         [],
 *     ],
 * }).result;
 * const sourceNamespace = std.coalesce({
 *     input: [
 *         sourceNamespaceConfig,
 *         collections.length > 0 ? collections[collections.length - 1].sourceNamespace : null,
 *     ],
 * }).result;
 * export const collectionStates = .map(c => ({
 *     sourceNamespace: c.sourceNamespace,
 *     targetNamespace: c.targetNamespace,
 *     effectiveTargetNamespace: c.effectiveTargetNamespace,
 *     state: c.state,
 *     indexStatus: c.indexStatus,
 *     documents: `${c.restoredDocuments} / ${c.totalDocuments}`,
 * }));
 * const thisGetCloudBackupCollectionRestoreJobCollection = mongodbatlas.getCloudBackupCollectionRestoreJobCollection({
 *     projectId: projectId,
 *     clusterName: clusterName,
 *     jobId: jobId,
 *     sourceNamespace: sourceNamespace,
 * });
 * export const collectionState = thisGetCloudBackupCollectionRestoreJobCollection.then(thisGetCloudBackupCollectionRestoreJobCollection => thisGetCloudBackupCollectionRestoreJobCollection.state);
 * ```
 *
 * ### Further Examples
 * - Restore job inspection
 * - Snapshot restore
 * - Point-in-time restore
 * - Snapshot discovery
 */
export function getCloudBackupCollectionRestoreJobCollectionsOutput(args: GetCloudBackupCollectionRestoreJobCollectionsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetCloudBackupCollectionRestoreJobCollectionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("mongodbatlas:index/getCloudBackupCollectionRestoreJobCollections:getCloudBackupCollectionRestoreJobCollections", {
        "clusterName": args.clusterName,
        "jobId": args.jobId,
        "projectId": args.projectId,
        "sourceNamespace": args.sourceNamespace,
        "state": args.state,
        "targetNamespace": args.targetNamespace,
    }, opts);
}

/**
 * A collection of arguments for invoking getCloudBackupCollectionRestoreJobCollections.
 */
export interface GetCloudBackupCollectionRestoreJobCollectionsOutputArgs {
    /**
     * Human-readable label that identifies the cluster with the collection restore job you want to return.
     */
    clusterName: pulumi.Input<string>;
    /**
     * Unique 24-hexadecimal digit string that identifies the collection restore job.
     */
    jobId: pulumi.Input<string>;
    /**
     * Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
     */
    projectId: pulumi.Input<string>;
    /**
     * Source namespace to filter by (e.g. `db.collection`).
     */
    sourceNamespace?: pulumi.Input<string | undefined>;
    /**
     * Collection-level state to filter by.
     */
    state?: pulumi.Input<string | undefined>;
    /**
     * Target namespace to filter by (e.g. `db.collection`).
     */
    targetNamespace?: pulumi.Input<string | undefined>;
}
